import { makeStyles } from "@material-ui/core";

export default makeStyles(theme => ({
  dayGroup: {
    display: "flex",
    // flexGrow: 1,
    // marginTop: "auto",
    padding: "1em",
    flexDirection: "column",
  },
  dividerText: {
    marginTop: "0.5em"
  }
}));