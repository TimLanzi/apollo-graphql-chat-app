import { makeStyles } from "@material-ui/core";

export default makeStyles(theme => ({
  minuteGroup: {
    display: "flex",
    flexDirection: "column",
    // padding: "0.3em 0"
  },
  minute: {
    color: "#888"
  }
}));